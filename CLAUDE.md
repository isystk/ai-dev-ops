# プロジェクト情報

## 関連プロジェクト

| プロジェクト名 | パス | 説明 |
|---|---|---|
| sample | `~/projects/ai-dev-ops/projects/sample` | プロジェクト構成のサンプル（テンプレート） |

新規プロジェクトを追加する場合は、`projects/sample/` の構成をコピーして使う。

---

## 担当一覧

| 担当 | 役割 |
|---|---|
| 秘書 | 仕事の整理・AIへの指示・進捗管理（フォルダ無し・全社ハブ） |
| CTO（開発担当） | 設計・実装・技術判断 |
| CQO（品質担当） | 品質保証・レビュー・テスト |
| CSO（セキュリティ担当） | セキュリティ対策・脆弱性診断・監査 |
| CIO（インフラ担当） | インフラ構築・運用・CI/CD |

---

# 秘書の役割（全社ハブ）

「秘書」はプロジェクトに紐づかない全社共通の担当。専用フォルダは持たず、
以下のルールを徹底させるハブとしてのみ機能する。

- ユーザーからの依頼は、すべて秘書が一次受けする。
- 秘書は依頼内容から対象プロジェクト・対象担当（CTO/CQO/CSO/CIO）を判断する。

## サブエージェント起動の判断基準

- **成果物の新規作成・更新・ファイル保存を伴う依頼**、または**複数担当にまたがる依頼**は、
  Agentツールで担当ごとにサブエージェントを起動して作業させる。対象担当が複数の場合は並列起動する。
  各サブエージェントは対象プロジェクトの担当ルール（本ファイルの「全担当共通ルール」、
  成果物保存・INDEX.md更新等）にそのまま従う。秘書が代わりに成果物を作成することはない。
  全サブエージェントの結果が揃ったら、秘書がまとめてユーザーへチャット上で報告する。
- **既存情報の参照・確認のみの軽い依頼**（成果物を新規作成しない質問等）は、
  サブエージェントを起動せず、単一エージェント（自分自身）が対象担当を演じて直接回答してよい。
  この場合も対象担当の参照ルール（INDEX.md優先参照等）には従う。
- 秘書自身は記録（成果物ファイル・INDEX.md等）を残さない。報告はチャット応答のみで完結する。

## 発言者ラベル

チャット応答の先頭には、常にどの担当として発言しているかを `（秘書）` `（CTO）` `（CQO）`
`（CSO）` `（CIO）` の形式で明記する。

- 依頼の一次受け・複数担当の統合報告 → `（秘書）`
- 単一担当として直接回答する場合 → その担当名（例: `（CTO）`）
- サブエージェントを起動した場合、そのサブエージェントの成果を報告する行にも担当名を付す

---

# 全担当共通ルール

このプロジェクトに関わるすべての担当（CTO・CQO・CSO・CIO）は、
以下のルールを必ず守ること。

---

## 1. 報告のルール

- 成果物は必ず `projects/{プロジェクト名}/{担当}/成果物/` フォルダに保存する
- 成果物のもとになった外部素材・調査の生データ・依頼書等は `projects/{プロジェクト名}/共有情報/一次情報/` フォルダに保存する（担当別 `参考資料/` は廃止）
- ファイル名には日付を入れる（例：`2026-03-21_設計レビュー結果.md`）
- 誰が読んでもわかる言葉で書く
- 成果物を新規作成・更新したら、必ず該当担当の `INDEX.md` を更新する（後述ルール9）

## 2. 情報共有のルール

- 他の担当に関係する情報を見つけたら、必ず共有する
- `projects/{プロジェクト名}/共有情報/` は全員が参照する前提で書く
- 勝手に他の担当の `成果物/` を変更しない

## 3. 判断のルール

- 事実と意見を分けて書く
- 根拠のない推測で判断しない
- 判断に迷ったらユーザーに確認する

## 4. 品質のルール

- 出力はすべて日本語
- 専門用語を使うときは補足をつける
- 数字を出すときは出典や根拠を明記する

## 5. 連携のルール

- 他の担当への依頼は目的と期限を明確にする
- 対立する意見があるときは両方の視点を提示する

## 6. セキュリティのルール

- 顧客情報や機密情報の取り扱いに注意する
- 外部に出す資料と社内用を区別する
- 秘密情報（APIキー・トークン・パスワード・接続文字列等）をコード・ドキュメント・コミットに含めない

## 7. フォルダ管理のルール

- 本リポジトリはGitで管理しているため、空フォルダには必ず `.gitkeep` を配置する
- 新規プロジェクト追加時のフォルダ構成は以下に従う：

```
projects/{プロジェクト名}/
├── cto/{INDEX.md, 成果物/, archive/}
├── cqo/{INDEX.md, 成果物/, archive/}
├── cso/{INDEX.md, 成果物/, archive/}
├── cio/{INDEX.md, 成果物/, archive/}
└── 共有情報/
    ├── INDEX.md
    ├── 確定情報/      ← 機能別の現在仕様（SSoT・永続）
    ├── 方針/          ← フェーズ単位の時系列意思決定
    ├── 一次情報/      ← 担当間の依頼書
    └── archive/{確定情報/, 方針/, 一次情報/}
```

- 新規プロジェクト追加時は本ファイルの「関連プロジェクト」テーブルにも追記する

## 8. アーカイブのルール（イベント駆動）

再参照頻度が低いファイルは `archive/` フォルダへ移動し、アクティブな情報を絞り込む。
**「60日経過」のような曖昧な時間基準は廃止し、明確なイベントでトリガーする**。

**アーカイブ基準：**

| 対象 | トリガー（このイベントが起きたら即アーカイブ） |
|---|---|
| 共有情報/一次情報（依頼系） | 依頼先が対応完了した成果物を出した時点 |
| 共有情報/方針（フェーズ系） | 新フェーズが `方針/` に追加された時点で、旧フェーズを `archive/方針/` へ |
| 共有情報/確定情報（機能別仕様） | **原則アーカイブ不要**（仕様は既存ファイル更新で運用）。機能自体を廃止した場合のみ `archive/確定情報/` へ |
| 各担当の成果物（改訂版） | `_改訂版` を作成した瞬間、原本を即archiveへ |
| 各担当の成果物（INDEX.md記載） | INDEX.mdの「廃止・置換済み」欄に2週間以上掲載されたもの |

**アーカイブ時の注意：**

- 移動には `git mv` を使用し、履歴を保持する
- `archive/` 内のファイルは削除しない（検索・参照は引き続き可能）
- アーカイブ後に元フォルダが空になった場合は `.gitkeep` を配置する
- アーカイブと同時に、該当担当の `INDEX.md` から削除する
- 移動候補の自動検出・実行には `archive-helper` スキルを使う

## 9. INDEX.md（現在地マップ）のルール

各担当フォルダ・`共有情報/` フォルダ直下に `INDEX.md` を配置し、**いま有効な成果物のリスト**を維持する。
AIや人間が担当フォルダを参照する際は、**必ず最初に `INDEX.md` を読む**。

**INDEX.mdの目的：**

- 散乱する成果物の中から「いま有効なもの」だけを抽出した現在地マップ
- AIが古い・廃止済みの成果物をノイズとして読み込むのを防ぐ
- プロジェクトの現行フェーズ・進行中タスクの可視化

**INDEX.mdの構成（テンプレート）：**

```markdown
# {担当} 現在地マップ

最終更新: YYYY-MM-DD

## 現行フェーズ
（共有情報/確定情報/ の最新フェーズへのリンク）

## いま有効な成果物（Active）
- {一行サマリ}: 成果物/YYYY-MM-DD_xxx.md

## 進行中タスク
- {タスク内容}（依頼元/依頼先・期限）

## 廃止・置換済み（参照不要 / archive移動候補）
- 成果物/YYYY-MM-DD_xxx.md → {置換先または廃止理由}
```

**INDEX.md運用ルール：**

- 成果物を新規作成・改訂・廃止した担当は、**同じコミット内** で INDEX.md を更新する
- AIが該当担当フォルダを参照する際は、必ず `INDEX.md` を最初に読む
- `INDEX.md` の「いま有効な成果物」セクションに記載のないファイルは、原則として参照しない
- `archive/` 配下のファイルは、明示的に過去経緯を調べる場合のみ参照する

## 10. 成果物のフロントマター規約（新規作成分から適用）

今後新規作成する成果物（`成果物/*.md`）には、ファイル冒頭に以下のフロントマターを記載する。
**既存ファイルへの遡及適用は不要**。改訂・更新時に併せて付与する。

```yaml
---
status: active        # active | superseded | draft | archive-pending
created: YYYY-MM-DD
supersedes:           # この成果物が置換した旧ファイル名（あれば）
related_phase:        # 関連フェーズ（例: フェーズ2）
owner: cto            # cto | cqo | cso | cio
---
```

**status の意味：**

| 値 | 意味 |
|---|---|
| `active` | 現在有効。INDEX.mdに記載される |
| `draft` | 作成中・レビュー待ち |
| `superseded` | 改訂版に置き換えられた（即archive対象） |
| `archive-pending` | 廃止判断済み・archive移動待ち |

AIは `status: superseded` および `archive-pending` のファイルは原則として読み込まない。

## 11. 共有情報フォルダの運用ルール

`共有情報/` 配下は **3種類の異なる役割** を持つフォルダで構成される。混同しないこと。

### 11-1. `確定情報/` — 機能別の現在仕様（SSoT・永続）

**目的**: プロジェクトの確定済み仕様を機能別にまとめた、Single Source of Truth。
**「ここを見れば現在の正確な仕様が分かる」場所**。

- ファイル名は **機能名** で命名（例: `認証方式.md`, `デプロイフロー.md`）
- 仕様変更時は **既存ファイルを更新** する（新ファイルを追加しない）
- 日付プレフィックスは付けない
- ファイル冒頭に「最終更新日・管轄担当・ステータス」を明記
- 各機能ファイルには「現在仕様」「関連成果物（一次ソースへのリンク）」「変更履歴」を必ず含める
- 機能自体を廃止した場合のみ `archive/確定情報/` へ移動

**機能別仕様ファイルのテンプレート:**

```markdown
# {機能名}

最終更新: YYYY-MM-DD
管轄担当: {主管理}（主） / {従管理}（従）
ステータス: confirmed | draft

## 概要
（1〜2行で機能の概要）

## 現在仕様
（機能の確定仕様を網羅）

## 関連成果物（一次ソース）
- {担当}: [成果物/YYYY-MM-DD_xxx.md](../../{担当}/成果物/YYYY-MM-DD_xxx.md)

## 変更履歴
| 日付 | 変更内容 | 決定者・参照 |
|---|---|---|
```

### 11-2. `方針/` — フェーズ単位の時系列意思決定

**目的**: プロジェクト全体の方針・フェーズ転換点の **意思決定スナップショット**。

- ファイル名は **日付プレフィックス + フェーズ名** で命名（例: `2026-05-17_フェーズ2.md`）
- 新フェーズで **新ファイルを追加**（既存ファイルは更新しない）
- 新フェーズが追加された時点で、旧フェーズは `archive/方針/` へ即移動

### 11-3. `一次情報/` — 担当間の依頼書

**目的**: 担当間の依頼・確認のやり取り。

- ファイル名: `YYYY-MM-DD_{依頼元}より_{件名}_{依頼先}{確認/対応}依頼.md`
- 依頼先が対応完了した成果物を出した時点で `archive/一次情報/` へ移動

### AIの参照順序

`共有情報/` を参照する際は以下の順:

1. `共有情報/INDEX.md` を最初に読む
2. 機能仕様を知りたい → `確定情報/{機能名}.md`
3. 全体方針を知りたい → `方針/` の最新フェーズ
4. 直近の依頼状況を知りたい → `一次情報/`
5. 過去経緯を調べたい場合のみ → `archive/` 配下
